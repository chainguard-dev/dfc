# Mapping of alpine packages name to equivalent Chainguard package(s)
alpine:
  python3-pip:
    - py3-pip:

# Mapping of debian packages name to equivalent Chainguard package(s)
debian:
  python3-pip:
    - py3-pip:

# Mapping of fedora packages name to equivalent Chainguard package(s)
fedora:
  python3-pip:
    - py3-pip:

